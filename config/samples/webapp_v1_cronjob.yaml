apiVersion: webapp.xilia.net/v1
kind: CronJob
metadata:
  labels:
    app.kubernetes.io/name: kops
    app.kubernetes.io/managed-by: kustomize
  name: cronjob-sample
spec:
  # TODO(user): Add fields here
